
// play a degree
await sequencer.play(/* degree = */ 1, /* bars = */ 1.5);

// start a degree
await sequencer.start(1);

const config = { 'mode': 'phrygian' };

const scale = [('m1', 'maj7'), (3,4)];
